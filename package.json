{
  "name": "cardforge-platform",
  "version": "1.0.0",
  "description": "Tournament management platform with Discord integration",
  "private": true,
  "scripts": {
    "dev": "concurrently \"pnpm --filter backend dev\" \"pnpm --filter bot dev\" \"pnpm --filter frontend dev\"",
    "build": "pnpm --recursive build",
    "deploy-commands": "pnpm --filter bot deploy-commands"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.1.11",
    "concurrently": "^8.2.2",
    "tailwindcss": "^3.4.17"
  },
  "dependencies": {
    "framer-motion": "^12.23.9",
    "lucide-react": "^0.526.0",
    "react-router-dom": "^7.7.1"
  }
}
